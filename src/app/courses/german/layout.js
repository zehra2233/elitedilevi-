export const metadata = {
  title: "German Courses | Elite Dil Evi",
  description:
    "German language courses from A1 to C2 with Goethe exam alignment at every level, in Istanbul.",
};

export default function Layout({ children }) {
  return children;
}
